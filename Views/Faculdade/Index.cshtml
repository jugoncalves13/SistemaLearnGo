@model IEnumerable<SistemaLearnGo.Models.Faculdade>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FaculdadeNome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FaculdadeCidade)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FaculdadeNome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FaculdadeCidade)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FaculdadeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FaculdadeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FaculdadeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
