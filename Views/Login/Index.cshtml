@model IEnumerable<SistemaLearnGo.Models.Login>

@{
    ViewData["Title"] = "Index";
}

<h1>Login</h1>

<p>
    <a asp-action="Create">Criar novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LoginEmail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LoginSenha)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LoginEmail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LoginSenha)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LoginId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.LoginId">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.LoginId">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
